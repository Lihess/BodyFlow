import {StyleSheet} from 'react-native';

const commonStyles = StyleSheet.create({
    Textbox : {
        flexDirection : 'row',
        alignItems : 'flex-end'
    }
});

export default commonStyles;