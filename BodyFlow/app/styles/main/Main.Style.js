import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    wrapper : {
        paddingHorizontal : '5%',
        paddingBottom : 0,
    },
    dotPosition : {
        bottom : 12
    }
});

export default styles;